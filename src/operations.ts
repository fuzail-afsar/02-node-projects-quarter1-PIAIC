// addition
export const addition = (num: number, num2: number) => num + num2;

// subtraction
export const subtraction = (num: number, num2: number) => num - num2;

// multiplication
export const multiplication = (num: number, num2: number) => num * num2;

// division
export const division = (num: number, num2: number) => num / num2;
